# Pinned to specific versions once the project stabilizes; for now, track
# what's been pip-installed into the conda "data" environment beyond what
# spec section 3 already lists as pre-installed (requests, pandas, numpy,
# SQLAlchemy, psycopg2, dbt-core, dbt-postgres, Jupyter, pyarrow).

faker
python-dotenv
